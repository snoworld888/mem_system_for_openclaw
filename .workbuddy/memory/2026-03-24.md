# 2026-03-24 工作日志

## 修复 test_assembler.py 唯一失败测试

**问题**: `TestAssemblerSwitches::test_stm_turns_zero_no_stm` 失败
- 期望 `stm_turns=0` 时 `short_term` 为空列表
- 实际返回了1条记录

**根因**: Python 的 `-0 == 0` 导致 `stm.turns[-0:]` 等价于 `stm.turns[0:]`，返回全部数据

**修复**:
1. `src/memory/short_term.py`: `get_recent_turns()` 开头增加 `if max_turns == 0: return [], stm.compressed_summary`
2. `src/memory/assembler.py`: `assemble()` 中对 `req.stm_turns <= 0` 提前判断，跳过 STM 查询

**结果**: 117 passed, 0 failed, 0 errors
- 各模块: Compressor(24) + STM(19) + LTM(21) + Assembler(19) + API集成(34) = 117
